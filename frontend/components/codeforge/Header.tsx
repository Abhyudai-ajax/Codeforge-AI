'use client';
import React,{useState} from 'react';
import {Search,Bell,Sparkles} from 'lucide-react';
export default function Header({title='CodeForge AI',onSearch,searchPlaceholder='Search problems, users...'}:{title?:string;onSearch?:(q:string)=>void;searchPlaceholder?:string}){const [q,setQ]=useState('');return <header className="h-16 shrink-0 border-b border-gray-800 bg-gray-950 px-5 flex items-center gap-5"><h1 className="text-lg font-bold text-white">{title}</h1><div className="flex-1 max-w-xl relative"><Search size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500"/><input value={q} onChange={e=>{setQ(e.target.value);onSearch?.(e.target.value)}} placeholder={searchPlaceholder} className="w-full rounded-lg border border-gray-800 bg-gray-900 py-2 pl-10 pr-4 text-sm text-white outline-none focus:border-blue-500"/></div><button className="text-gray-400 hover:text-white"><Sparkles size={19}/></button><button className="relative text-gray-400 hover:text-white"><Bell size={19}/><span className="absolute -right-1 -top-1 h-2 w-2 rounded-full bg-blue-500"/></button></header>}
